{
  "$schema": "https://railway.com/railway.schema.json",
  "build": {
    "builder": "NIXPACKS",
    "buildCommand": "npm install --legacy-peer-deps --ignore-scripts && ./node_modules/.bin/prisma generate && npm run build"
  },
  "deploy": {
    "startCommand": "./node_modules/.bin/prisma migrate deploy && node dist/src/index.js",
    "healthcheckPath": "/health",
    "restartPolicyType": "ON_FAILURE"
  }
}
