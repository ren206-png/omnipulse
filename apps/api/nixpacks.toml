[phases.setup]
nixPkgs = ["nodejs_20"]

[phases.install]
cmds = ["npm install --legacy-peer-deps --ignore-scripts"]

[phases.build]
cmds = ["./node_modules/.bin/prisma generate", "npm run build", "cp -r generated dist/"]

[start]
cmd = "node dist/src/index.js"
